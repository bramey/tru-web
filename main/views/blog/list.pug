extends ../layout
block title
  title Nullivex Blog Posts
block content
  .container
    h1 Nullivex Blog
    p
      | Technology related blog posts about situations we have encountered and
      | solved. As well as outlook on the future of technology, code, security
      | considerations and network engineering as we grow.
    .content.content-container
        each blog in blogList
          .blogListEntry
            h3(style='margin-top: 5px;')
              a(href='/blog/' + blog.uri)= blog.title
            p(style='margin-top:-8px')
              | Posted: #{moment(blog.datePosted).format('MMM DD, YYYY')}
              | by <a href="#{blog.authorUrl ? blog.authorUrl : 'http://www.nullivex.com'}">#{blog.author}</a>
            div(style='margin-top:15px;margin-bottom:10px')
              if null !== blog.coverPhotoUrl
                img.pull-left(src=blog.coverPhotoUrl, style='width: 200px; margin: 10px;')
              p= blog.description
              a(href='/blog/' + blog.uri) Read more...
            .clear-both
